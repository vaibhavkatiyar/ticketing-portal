import { IdSearchFilterPipe } from './id-search-filter.pipe';

describe('IdSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IdSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
