import { BugDetailsFilterPipe } from './bug-details-filter.pipe';

describe('BugDetailsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BugDetailsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
